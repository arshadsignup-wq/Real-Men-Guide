'use client';

import { getToolBySlug } from '@/data/tools-registry';
import { generateToolJsonLd, generateFaqJsonLd, generateBreadcrumbJsonLd } from '@/lib/seo';
import ToolPageWrapper from '@/components/tool-page/ToolPageWrapper';
import CalculatorEngine from '@/components/engines/CalculatorEngine';
import { cleanSheetsCalculatorConfig } from '@/data/tools/clean-sheets-calculator';

const tool = getToolBySlug('clean-sheets-calculator')!;

export default function CleanSheetsCalculatorPage() {
  return (
    <>
      <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(generateToolJsonLd(tool)) }} />
      <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(generateFaqJsonLd(cleanSheetsCalculatorConfig.supportingContent.faq)) }} />
      <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(generateBreadcrumbJsonLd(tool)) }} />
      <ToolPageWrapper tool={tool} supportingContent={cleanSheetsCalculatorConfig.supportingContent}>
        <CalculatorEngine config={cleanSheetsCalculatorConfig} tool={tool} />
      </ToolPageWrapper>
    </>
  );
}
